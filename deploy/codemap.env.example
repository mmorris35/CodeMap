# CodeMap API Environment Configuration
# Copy this file to /etc/codemap/env and customize for your deployment
#
# Usage:
#   sudo cp deploy/codemap.env.example /etc/codemap/env
#   sudo nano /etc/codemap/env
#   sudo chown root:codemap /etc/codemap/env
#   sudo chmod 640 /etc/codemap/env

# Deployment Environment
# Values: production, staging, development
CODEMAP_ENV=production

# Logging Level
# Values: DEBUG, INFO, WARNING, ERROR, CRITICAL
CODEMAP_LOG_LEVEL=INFO

# Results Storage Directory
# Path where job results are stored locally
# Must be writable by codemap user
CODEMAP_RESULTS_DIR=/opt/codemap/results

# AWS Configuration
# Default region for AWS operations
AWS_DEFAULT_REGION=us-west-2

# Storage Backend
# Values: local (default) or s3
# Use 'local' for development or single-instance deployments
# Use 's3' for multi-instance setups or data persistence across instances
CODEMAP_STORAGE=local

# S3 Storage Configuration (only needed if CODEMAP_STORAGE=s3)
# S3 bucket name for storing analysis results
# Format: codemap-results-{AWS_ACCOUNT_ID}
# CODEMAP_S3_BUCKET=codemap-results-123456789012

# AWS Credentials (only needed if not using IAM roles)
# Recommended: Use IAM role attached to EC2 instance instead
# If using explicit credentials, set both:
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key

# API Server Configuration (advanced)
# These are usually set by systemd service, but can be overridden here
# CODEMAP_HOST=0.0.0.0
# CODEMAP_PORT=8000
# CODEMAP_WORKERS=2

# Timezone (for logging and timestamps)
# TZ=UTC
